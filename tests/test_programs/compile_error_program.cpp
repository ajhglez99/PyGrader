#include <iostream>
#include <bits/stdc++.h>

using namespace std;

/**
 * @file compile_error_program.cpp
 * @brief This program is intentionally written with a syntax error for testing purposes.
 *
 * The program lacks a semicolon after the return statement in the main function.
 * This is done to test if the package can properly detect compilation failures.
 */

int main()
{
    return 0 
}
